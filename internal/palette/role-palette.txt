Generated by `agent-compose palette-snapshot`. Do not edit.

Each role's favorite color is derived from the whole roster at once, not
from its own personalities alone. Roles that share personalities have
nearly identical component means, so a per-role average cannot tell them
apart.

Two stages. Each role first blends its components weighted by how few other
roles share them, so whatever distinguishes a role leads its color. Then
every role is pushed away from its nearest neighbor to grow the smallest
pairwise distance, bounded by a drift cap so no color leaves its own blend,
and clamped back into the terminal-legible band. Iteration order is fixed
and nothing is random, so one roster always derives one palette.

Roster loading asserts a floor on the closest pair. Two roles melding the
same personalities derive barely 0.06 apart and fail it.

The derivation is global, so editing one role can move every color below.
That is why this file is committed: the change arrives as a diff.

The window background is a second derivation from the same set. Accents
live in the legible band, so tinting one into a near-black leaves seven
backgrounds nobody can tell apart. Each role's hue is held and moved onto
equal spacing at a fixed low lightness and chroma, at the rotation offset
that turns every role the least. agentic-os#1245, #358.

Lightness buys that spacing and chroma does not. Adjacent hues sit closer
as the roster grows, and more chroma at a low lightness pushes them out of
sRGB where they clamp back together. Measured while adding the tenth seat:
at L 0.235 the closest pair fell to 0.0267 against a 0.0300 floor, raising
chroma to 0.070 recovered only 0.0025, and L 0.260 cleared it outright.

Closest accent pair: 0.0810
Closest background pair: 0.0319

platform // #709b62 // #0e2b14 // tenacious, grounded
sysadmin // #0d9c92 // #002a36 // protective, grounded
science // #14d5b8 // #002c27 // empirical, grounded
frontend // #ee7eea // #321a31 // playful, imaginative
gamedev // #3b82ff // #11243e // immersed, imaginative
director // #da656f // #391721 // decisive, outward
advocate // #f7a060 // #3a1a0e // warm, outward
analyst // #9190c1 // #251e3c // suspicious, tenacious
psych // #ab9137 // #332000 // warm, protective
reporter // #99b052 // #252602 // tenacious, outward
