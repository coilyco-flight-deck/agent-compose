# Generic person-profile example

This public fixture demonstrates local profile authoring with an admitted local
personality library. It contains no customer material.

Run `agent-compose compose request.kdl --out ./bundle`, then use `evaluation`,
`overlay`, `roster`, `catalog`, `describe`, and `verify` against the same local
profile and `../shared-personality-library` root.
