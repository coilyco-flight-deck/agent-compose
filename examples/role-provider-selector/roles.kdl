repositories {
    repository hardware path="example/hardware-knowledge" {
        skill "compute-stack"
        skill "machine-*"
    }
}

roles {
    role platform {
        use-repository hardware
    }
}
