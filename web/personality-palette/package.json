{
  "name": "agent-compose-personality-palette",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "test": "npm run build && node --test tests/build.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "8.0.13"
  }
}
